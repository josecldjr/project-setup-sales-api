import { Router } from "express";


const routes  = Router()

routes.get('/', (request, response) => {
  return response.json({message: 'kIDksaikdias kdi kas dikasi dkias kdiaks das idk'})
})



export default routes
